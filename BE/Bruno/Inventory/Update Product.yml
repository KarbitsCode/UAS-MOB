info:
  name: Update Product
  type: http
  seq: 3

http:
  method: POST
  url: "{{BACKEND_URL}}/index.php?resource=inventory&apicall=updateData"
  params:
    - name: resource
      value: inventory
      type: query
    - name: apicall
      value: updateData
      type: query
  body:
    type: json
    data: |-
      {
        "id_produk":3,
        "nama_produk": "update",
        "harga": 99999,
        "stok": 999
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
