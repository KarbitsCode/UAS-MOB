info:
  name: Update Pesanan
  type: http
  seq: 3

http:
  method: POST
  url: "{{BACKEND_URL}}/index.php?resource=pesanan&apicall=updateData"
  params:
    - name: resource
      value: pesanan
      type: query
    - name: apicall
      value: updateData
      type: query
  body:
    type: json
    data: |-
      {
        "id_transaksi": 4
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
